template<typename T, typename U>
bool Collider<T, U>::checkCollision(const T& object1, const U& object2) {
    // Implementação do verificador de colisão entre os objetos T e U
    // Retorne true se houver colisão, false caso contrário
    // Exemplo de implementação:
    // return object1.getBoundingBox().intersects(object2.getBoundingBox());
    return false; // Implementação de exemplo
}

